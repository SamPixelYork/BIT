----

[toc]

----

#### Q1. How many stars will be displayed in the following code if $`n`$ is 10? How many stars if $`n`$ is 20? What is the complexity?

```java
for (int i = 0; i < n; i++) {
  for (int j = 0; j < n; j++) {
    System.out.print('*');
  }
}
```

- [ ] $`10, 20, O(n)`$
- [ ] $`100, 200, O(n^2)`$
- [x] $`100, 400, O(n^2)`$ :white_check_mark:
- [ ] $`100, 400, O(n)`$

> 0.5 points

#### Q2. What will display by the following code?

```java
class Test {
  public static void main(String[] args) {
    int[] list1 = {3, 2, 1};
    int[] list2 = {1, 2, 3};
    list2 = list1;
    list1[0] = 0; list1[1] = 1; list2[2] = 2;

    for (int i = list2.length - 1; i >= 0; i--) {
      System.out.print(list2[i] + " ");
    }
  }
}
```

- [ ] 3 2 1
- [ ] 0 1 2
- [ ] 1 2 3
- [x] 2 1 0 :white_check_mark:

> 0.5 points

#### Q3. Assume that you are writing a class *Student* and wish to implement the *Comparable\<Student\>* interface. What method must the *Student* class contain?

- [ ] Student compareTo(Object stu)
- [ ] boolean compareTo(T stu)
- [ ] int compareTo(Object stu)
- [x] int compareTo(Student stu) :white_check_mark:

> 0.5 points

#### Q4. Compute the *Big-O* notation for the following code

```java
int count = 1;
do {
  count *= 2;
} 
while (count < n);
```

- [x] $`O(n)`$ :negative_squared_cross_mark: :
- [ ] $`O(\log_2n)`$ :white_check_mark:
- [ ] $`O(n\log_2n)`$
- [ ] $`O(n^2)`$

> 0 points

#### Q5. Why is a non-dominating term ignored in the *Big-O* notation?

- [x] because as the input size grows, the dominating term grows much faster than the non-dominating term :white_check_mark:
- [ ] because it decreases the growth rate
- [ ] because it has no impact on the growth rate of the time complexity function
- [ ] because it increases the growth rate

> 0.5 points

#### Q6. Which of the following is the postfix conversion of *3 ^ 9 / (5 * 4) + 8*?

- [ ] 3 9 ^ 5 4 * / + 8
- [ ] 3 9 ^ 5 4 / * + 8
- [ ] 3 9 ^ 5 4 / * 8 +
- [x] 3 9 ^ 5 4 * / 8 + :white_check_mark:

> 0.5 points

#### Q7. Which of the following is postfix conversion of *5 + 3 * 2*?

- [ ] 5 3 + 2 *
- [x] 5 3 2 * + :white_check_mark:
- [ ] 5 3 2 + *
- [ ] 5 3 + * 2

> 0.5 points

#### Q8. What will be the output of the following code

```java
public static void main(String[] args) {
  int[] list1 = {1, 2, 3};
  int[] list2 = {4, 5, 6};
  list2 = list1.clone();
  list1[0] = 6; list1[1] = 7; list1[2] = 8;
  System.out.println(list2[2]);
}
```

- [ ] 6
- [ ] 4
- [ ] 8
- [x] 3 :white_check_mark:

> 0.5 points

#### Q9. Which is the most correct statement?

- [ ] A LinkedList is considered empty if it contains nothing but a head node.
- [x] A stack and a queue can be used together to check if a word is a palindrome. :white_check_mark:
- [ ] A queue is a useful data structure for reversing strings.
- [ ] All data is added to a stack from the rear and polled off the front.

> 0.5 points

#### Q10. Which of the following data structures is primarily used for checking *Balanced Parentheses*?

- [x] Stack :white_check_mark:
- [ ] Queue
- [ ] Priority Queue
- [ ] None of the above

> 0.5 points

#### Q11. What is the *Big-O* notation of $`n(11n + 3n\log_2n) + n + 19`$?

- [ ] $`O(n^2)`$
- [ ] $`O(\log_2n)`$
- [x] $`O(n\log_2n)`$ :negative_squared_cross_mark:
- [ ] $`O(n^2\log_2n)`$ :white_check_mark:

> 0 points

#### Q12. Which one of the following statements is *NOT* true?

- [ ] Generics enable you to detect errors at compile time rather than at runtime.
- [x] Generics let you parameterise types. :negative_squared_cross_mark:
- [ ] Generic type can be replaced with a primitive type. :white_check_mark:
- [ ] None of the above.

> 0 points

#### Q13. Below are two implementations for checking whether a number *n* is prime or not. Which of the following is a valid statement for these implementations related to worst-case time analysis?

```java
impl-1:
public boolean isPrime(int n) {
  for (int i = 2; i < n; ++i) {
    if (n % i == 0)
      return false;
  }
  return true;
}
```

```java
impl-2:
public boolean isPrime(int n) {
  for (int i = 2; i < Math.sqrt(n); ++i) {
    if (n % i == 0)
      return false;
  }
  return true;
}
```

- [x] impl-1 is better impl-2 :negative_squared_cross_mark:
- [ ] impl-2 is better than impl-1 :white_check_mark:
- [ ] Their performance will be the same
- [ ] None of the above

> 0 points

#### Q14. Evaluate the following postfix equation: *5 3 2 * + 4 - 5 +*

- [ ] 54
- [ ] 29
- [ ] 30
- [x] 12 :white_check_mark:

> 0.5 points

#### Q15. What is the precondition of a method?

- [ ] The status of the calling method
- [x] The condition that must be true when calling a method :white_check_mark:
- [ ] The first instance variable of a class
- [ ] None of the above.

> 0.5 points

#### Q16. What is true for the below *addNode(...)* method?

```java
public class Node<E> {
  private E data;
  private Node<E> link;

  public Node(E initialData, Node initialLink) {
    data = initialData;
    link = initialLink;
  }
  public void addNode(E element) {
    link = new Node<E>(element, link);
  }
}
```

- [ ] The method adds a new node before this node.
- [x] The method adds a new node after this node. :white_check_mark:
- [ ] All of the above.
- [ ] None of the above.

> 0.5 points

#### Q17. What is true for the below *add(...)* method?

```java
public class IntNode {
  private int data;
  private IntNode link;

  public IntNode(int initialData, IntNode initialLink) {
    data = initialData;
    link = initialLink;
  }
}
public class LinkedBag {
  private IntNode head;
  private int manyNodes;

  public IntLinkedBag() {
    head = null;
    manyNodes = 0;
  }

  public void add(int element) {
    head = new IntNode(element, head);
    manyNodes++;
  }
}
```

- [x] The method adds a new node at the front of the linkedbag. :white_check_mark:
- [ ] The method adds a new node at the end of the linkedbag.
- [ ] All of the above.
- [ ] None of the above.

> 0.5 points

#### Q18. Predict the output of the following program

```java
class TestType implements Cloneable {
  int a;
  TestType clone() {
    try {
      return (TestType) super.clone();
    }
    catch (CloneNotSupportedException e) {
      System.out.println("CloneNotSupportedException is caught");
      return this;
    }
  }
}

class Main {
  public static void main(String args[]) {
    TestType obj1 = new TestType();
    TestType obj2;
    obj1.a = 20;
    obj2 = obj1.clone();
    obj2.a = 30;

    System.out.println("obj1.a = " + obj1.a);
    System.out.println("obj2.a = " + obj2.a);
  }
}
```

- [ ] obj1.a = 20; obj2.a = 20
- [x] obj1.a = 20; obj2.a = 30 :white_check_mark:
- [ ] obj1.a = 30; obj2.a = 20
- [ ] obj1.a = 30; obj2.a = 30

> 0.5 points

#### Q19. Which one of the following time analyses (in general) indicates the best performance?

- [ ] $`O(n)`$
- [x] $`O(\log_2n)`$ :white_check_mark:
- [ ] $`O(n\log_2n)`$
- [ ] $`O(n^2)`$

> 0.5 points

#### Q20. Which of the following data structure(s) is(are) generally used for checking palindrome?

- [ ] Stack
- [ ] Queue
- [x] Both Stack and Queue :white_check_mark:
- [ ] None of the above

> 0.5 points

#### Q21. What is a data structure?

- [ ] A collection of data organised in some fashion, and the organisation of the structure determines what operations the structure can support.
- [ ] In object-oriented thinking, a data structure is an object that stores other objects, referred to as data or elements. So some people refer to a data structure as a container object or a collection object.
- [x] Both (a) and (b). :white_check_mark:
- [ ] None of the above.

> 0.5 points

#### Q22. Which of the following is an *Abstract Data Types* in Java?

- [ ] A Java class with methods
- [x] ArrayList :white_check_mark:
- [ ] double
- [ ] String

> 0.5 points

#### Q23. Queue is

- [x] FIFO. :white_check_mark:
- [ ] LIFO.
- [ ] Both LIFO and FIFO.
- [ ] None of the above.

> 0.5 points

#### Q24. Count the number of iterations in the following loop

```java
int count = 1;

while (count < 30) {
  count = count * 2;
}
```

- [x] 5 :white_check_mark:
- [ ] 15
- [ ] 30
- [ ] None of the above.

> 0.5 points

#### Q25. What will be the value of *i* after the following for loop is finished?

```java
int y = 0, i;
for (i = 0; i < 10; ++i) {
  y += ;
}
```

- [ ] 9
- [x] 10 :white_check_mark:
- [ ] 11
- [ ] Undefined.

> 0.5 points

#### Q26. Stack is

- [ ] FIFO.
- [x] LIFO. :white_check_mark:
- [ ] Both LIFO and FIFO.
- [ ] None of the above.

> 0.5 points

#### Q27. What interface a class should implement to be able to support enhanced for loop, i.e., *for (data_type item: collections) {}*

- [ ] Abstract
- [ ] Cloneable
- [x] Iterable :white_check_mark:
- [ ] Comparable

> 0.5 points

#### Q28. In general, an *Interface* comprises of (or can have)?

- [ ] Classes and constants.
- [ ] Methods and variables.
- [x] Methods and constants. :white_check_mark:
- [ ] Only methods.

> 0.5 points

#### Q29. Why is a constant factor ignored in the *Big-O* notation?

- [ ] Because it increases the growth rate.
- [ ] Because it decreases the growth rate.
- [ ] Because it has no impact on the growth rate of the time complexity function :white_check_mark:
- [x] Because, as the input size grows, the dominating term grows much faster than the non-dominating term. :negative_squared_cross_mark:

> 0 points

#### Q30. *Iterator* is an example of?

- [ ] Creational patterns.
- [x] Structural patterns. :negative_squared_cross_mark:
- [ ] Behavioural patterns :white_check_mark:
- [ ] None of the above.

> 0 points
